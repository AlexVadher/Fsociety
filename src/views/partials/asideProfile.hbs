 <!-- Sidebar -->
<aside class="w-64 bg-white shadow-lg p-6">
  <h2 class="text-xl font-semibold text-gray-700 mb-6">Configuración de Perfil</h2>
  <nav class="space-y-4">
    {{#each menuItems}}
      <a href="{{this.link}}" class="block px-4 py-2 text-sm text-gray-700 {{#if this.active}}bg-gray-200{{/if}} rounded-md hover:bg-gray-300">
        <i class="{{this.icon}} mr-2"></i>
        {{this.name}}
      </a>
    {{/each}}
  </nav>
</aside>
